spring:
  application:
    name: nbapickem

  datasource:
    url: jdbc:postgresql://localhost:5433/nbapickem
    username: nbapickem
    password: nbapickem123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 8080

logging:
  level:
    org.springframework.security: DEBUG

